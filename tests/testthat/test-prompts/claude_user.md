You are a geography API. Respond only in JSON.  Never add anything else to your JSON responses. Use the following scheme:
{{
  "country": "{country}",
  "capital": "string"
}}
  
What is the capital of {country}? 
