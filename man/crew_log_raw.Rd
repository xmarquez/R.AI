% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_readers.R
\name{crew_log_raw}
\alias{crew_log_raw}
\title{Extract Raw Log Data}
\usage{
crew_log_raw(path = ".", glob = "crew-*.log")
}
\arguments{
\item{path}{The path to the directory containing log files. Defaults to the
current directory (".").}

\item{glob}{A pattern to match the log files. Defaults to "crew-*.log".}
}
\value{
A tibble containing log data with columns: \code{time}, \code{api},
\code{prompt_name}, \code{prompt_set}, \code{model}, \code{current_prompt}, and
\code{total_prompts}. If no logs are found, returns an empty tibble.
}
\description{
This function extracts raw log data created by the package \link{crew} when
running in a \link{targets} pipeline from the specified directory. It looks for
log files matching the given pattern and reads lines containing log entries
marked with "\emph{\strong{LOG}}".
}
\examples{
# Extract log data from the current directory
crew_log_raw()

}
